apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  env: dev
  platform: eks

resources:
  - ../../k8s

patches:
  - path: patch-configmap.yaml
  - path: patch-namespace-label.yaml
